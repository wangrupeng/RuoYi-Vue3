# 三维模型重建系统 - 实现思路

## 1. 总体架构

系统采用 **Mission-Device-Trajectory 三层架构**，以应对工业现场复杂的设备更换、断网续传等场景：

- **Mission（检测任务）**：一次完整的爬升检测任务，包含任务ID、塔基坐标、状态等
- **Device（硬件设备）**：真实的物理设备，含设备ID、类型、校准参数。与 `robotId` 解耦，支持换设备续爬
- **Trajectory（轨迹数据）**：按 `robotId`（逻辑身份）组织，一个逻辑机器人可包含多段 `subTrajectory`（子轨迹）

---

## 2. 数据协议设计

后端通过 WebSocket 实时传输 GPS 坐标，前端定义统一的数据结构：

```
{
  missionId, deviceId, robotId,
  sequence, timestamp, receiveTime,
  longitude, latitude, altitude,
  status, signalQuality, battery
}
```

**双时间轴设计**：
- `timestamp`：业务采集时间，用于排序建模
- `receiveTime`：网络接收时间，用于检测断网/补传（`receiveTime >> timestamp` 表示补传数据）

---

## 3. 前端模拟器设计

由于后端尚未实现，前端通过 `setInterval(200ms)` 自研模拟器生成 GPS 数据流：

### 3.1 轨迹生成模式

采用**垂直扫描模式**：机器人沿一条垂直母线从底部爬到顶部，到达端点后进行水平偏移，再反向爬升，如此反复直至覆盖整个目标表面。

| 模型类型 | 水平偏移语义 | 完成条件 |
|---------|------------|---------|
| 塔 / 风机 | 绕塔方位角（弧度） | 累计旋转 ≥ 360° |
| 立面 | 墙面水平位置（米） | 累计扫描 ≥ 墙面宽度 |

### 3.2 多机器人协同

- 每台机器人从不同的起始角度/位置出发（均匀分配）
- 独立的状态机：`running` / `idle` / `finished`
- 独立的数据缓存和轨迹可视化

---

## 4. 数据接入引擎

数据从模拟器（或真实 WebSocket）进入后，经过以下处理流水线：

```
原始数据 → 字段校验 → 缓冲区 → 飞点过滤 → 去重检测 → 子轨迹切分 → 存储 → UI更新 → 实时可视化
```

### 4.1 字段校验层
校验字段存在性、类型、经纬度范围（-180~180, -90~90）、海拔范围（-500~10000m）、missionId 匹配。

### 4.2 飞点过滤
计算相邻点的物理速度，超过 `50m/s` 阈值判定为飞点并丢弃。

### 4.3 去重检测
在 `1s` 时间窗口内，空间距离 `< 5cm` 的点合并为 idle 点。

### 4.4 子轨迹自动切分
当相邻点时间差 `> 5s`，或空间差 `> 2m` 且时间 `> 1s` 时，自动切分为新的子轨迹。用于处理断电停机、网络断连后的续爬场景。

---

## 5. 设备校准与轨迹质量

### 5.1 设备校准
利用两台设备轨迹的重叠段，计算平均圆心偏移量 `(offsetX, offsetY, offsetZ)`。换设备后续爬时，新设备的轨迹自动应用校准偏移。

### 5.2 轨迹质量评分
三维度加权评分：
- **密度**（40%）：有效点占总点比例
- **连续性**（40%）：子轨迹数量越少越好
- **信号质量**（20%）：GPS 信号强度均值

---

## 6. 建模算法

系统支持三种模型类型，各自采用不同的建模策略：

### 6.1 塔（Tower）
塔为旋转体，截面近似圆形，采用**分层圆拟合 + 段拼接**算法：

1. **分层切片**：按高度 0.5m 一层，将所有有效点分配到各层
2. **RANSAC 圆拟合**：对每层点的 `(x, z)` 坐标进行圆拟合，先均值估计中心，再以 `2σ` 剔除离群点，重新计算半径
3. **段边界识别**：遍历各层半径，变化率 `> 0.5m` 处判定为段边界（如变截面、平台、哑铃型腰部）
4. **网格生成**：每段用 `CylinderGeometry(topR, bottomR, height)` 生成，多段拼接成完整塔模型

### 6.2 风机（Wind Turbine）
风机塔筒部分与塔共用同一套建模算法（旋转体圆柱拟合）。顶部机舱简化为附加几何体，目前以塔筒重建为主。

### 6.3 立面（Facade）
立面为建筑物外墙，近似平面，采用**边界框拟合**算法：

1. 收集所有有效点的 `(x, y, z)` 坐标
2. 计算三个轴向上的边界框（`minX/maxX`, `minY/maxY`, `minZ/maxZ`）
3. 生成 `BoxGeometry(width, height, depth)` 作为墙体主体
4. 添加 `EdgesGeometry` 边框线增强可视化效果

---

## 7. 异常场景处理

模拟器支持参数化注入以下异常场景：

| 类别 | 异常场景 | 处理策略 |
|------|---------|---------|
| 设备异常 | 断电停机、换设备续爬 | 子轨迹切分、设备校准 |
| 信号异常 | GPS重复、飞点、位置跳变 | 去重检测、飞点过滤 |
| 网络异常 | 断网缓存、补传历史点 | 双时间轴识别、补传队列 |
| 环境异常 | 遮挡、多路径、磁场干扰 | 信号质量评分标记 |

---

## 8. Three.js 可视化

### 8.1 实时轨迹
- `BufferGeometry` + `Float32Array` 预分配 10 万点
- `setDrawRange` 动态控制显示范围，避免每帧重建
- 自动扩容机制（容量不足时翻倍）

### 8.2 模型重建
- 塔：多段半透明圆柱 + 顶部/底部圆圈线框
- 立面：半透明 Box + 绿色边框线
- 支持线框/实体模式切换

### 8.3 场景控制
- `OrbitControls` 阻尼旋转/缩放/平移
- `GridHelper` 网格地面辅助定位
- 截图导出（`preserveDrawingBuffer`）

---

## 9. 模型序列化与存储

重建完成后，模型序列化为 JSON 结构：

```
{
  modelId, modelName, missionId, createTime,
  modelType, towerProfile, totalHeight, dataCompleteness,
  segments[], trajectories[], devices[], missingSegments[]
}
```

- `trajectories` 包含降采样后的 GPS 点数组（每 5 个点保留 1 个）
- 目前后端未实现，通过 `console.log` 打印 + `Blob` 下载 JSON 文件

---

## 10. 历史模型管理

- 查询表单：模型名称、模型类型、创建时间
- 表格展示：模型名称、任务ID、模型类型、设备数、轨迹数、总高度、完整度、缺失段、创建时间
- 操作：3D回显（加载轨迹到场景）、对比（双模型差异分析预留）、下载（JSON导出）、删除
